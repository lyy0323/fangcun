# v3.0 发版清单（2026-09-09）

> 用途：web + Android v3.0 发布核对。checker 部分由仓库主自行执行（惯例不改 checker）。

## A. shiva 仓库（本仓，dev → main 已处理）
- [x] 版本号：Android `versionCode 17 / versionName "3.0"`（android/app/build.gradle）
- [x] CHANGELOG.md 增补 v3.0 条目
- [x] TopBar.tsx 更新日志增补 v3.0 条目
- [x] 依赖口径：pyproject.toml `psycopg2-binary>=2.9` → `psycopg[binary]>=3.1`（与 requirements.txt/代码实际 import psycopg3 一致）
- [x] 后端测试：`python3 -m pytest tests/` → 395 passed
- [x] 前端：`npm run build`（tsc + vite）通过；`npm audit --omit=dev` → 0 漏洞
- [x] API 文档 static/docs.html 核对/补全（上古五书 + char/definitions 端点）
- [x] 合并 dev → main 并 push origin（含 dev 提交 + release 改动，c66dfc1）
- [ ] `vercel --prod` 部署 web（注意：非 GitHub 自动部署）
- [ ] `bash test_api.sh` 生产验证（write.sjtuguoxue.space）

## B. checker 仓库（fangcun-checker，主自行执行）
- [ ] 提交工作区未提交改动（app.py +121 行：上古诗经/楚辞双套 VALID_BOOKS + 句末语气词启发式；static/config 数据 23MB 多文件）
- [ ] checker.sjtuguoxue.space 部署新 checker（v3.0 web 依赖它提供 51/36 上古韵校验与 lookup）
- [ ] shiva 的 vercel.json 已把 6 条 checker 路由由「边缘 rewrite」改为「app.py 透传」——需确认 checker 域名可达、proxy 后鉴权/限额仍生效
- [ ] 注意：checker 工作区 `static/config/shi_rules.json`（4/30）与 shiva 已提交版（8/14）存在 8 条诗格规则差异（七律/七绝/五律/五绝 首句入韵各 2 类）——两仓对齐后再发 checker，避免参考站诗格页与线上校验不一致
- [ ] checker API 文档（checker 侧 static/docs.html）同步五书 + 上古参数（如 strip_particles）

## C. API 文档 review 遗留（docs 子代理报告，需发版人确认）
- [ ] docs.html「限流」表与代码不符（既有）：app.py 限流默认关闭（RATELIMIT_ENABLED=1 才启用），启用仅 default 60/min 且 validate_meter/free_rhyme/rhyme.lookup/rhyme.list/rules.list/char.lookup 均 @limiter.exempt；docs 却写 validate/free 60/min、dictionary.search/allusion 120/min；checker 侧默认 600/min——三处不一致，建议按生产实际配置定稿
- [ ] docs.html 侧边栏版本角标「v0.1」与 v3.0 不符（纯展示，未改）
- [ ] rules/list 无 book/rhyme_book_name 参数（checker 实现仅 genre/search），已核对无需改
- [ ] 上古两书读音/韵部细节（readings、details、tone_type 均 Z、51/36 命名）按本地 main + static/config 核对——若生产 checker 部署版本不同需复核

## D. 遗留提示（本次不阻塞，另行评估）
- [ ] shiva 工作区存在多个未跟踪文件：`frontend/public/fonts/{ml,LXGWWenKaiLite-Regular,NotoSansSC-Regular,方正宋刻本秀楷简体}`（~43MB）——用户确认 web 字体走阿里云 CDN，不入库
- [ ] 多个未跟踪脚本/测试/文档（build_definitions.py、build_tongwei.py、create_full_metadata.py、migrate_dict_to_neon.py、tests/conftest.py 等 7 个 test、docs/*.md）——是否入库由主决定
- [ ] `data/`（api_keys.db 等）gitignore 未跟踪，保持忽略
